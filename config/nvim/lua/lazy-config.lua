return {
	require("lazy").setup({
		require("lazy.autocomplete"),
		require("lazy.conform"),
		require("lazy.comment"),
		require("lazy.oil"),
		require("lazy.indent_line"),
		require("lazy.gitsigns"),
		require("lazy.lspconfig"),
		require("lazy.mini"),
		require("lazy.startup"),
		require("lazy.telescope"),
		require("lazy.theme"),
		require("lazy.todo"),
		require("lazy.tree-sitter"),
		require("lazy.vim-helm"),
		require("lazy.vim-sleuth"),
	}, {}),
}
